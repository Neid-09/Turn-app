spring:

  cloud:
    config:
      enabled: false

  application:
    name: =turnos-microservice

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9090/realms/myturnapp-microservices-realm